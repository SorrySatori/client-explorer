# Raynet CRM API access — used by the /api server route (and copied into
# process.env by vite.config.ts in dev). Never expose these to the client.
RAYNET_API_URL=https://app.raynet.cz/api/v2
RAYNET_BEARER_TOKEN=your-bearer-token
